with "config/rongen_config.gpr";
project Rongen is

   for Source_Dirs use ("src/**", "config/");
   for Object_Dir use "obj/" & Rongen_Config.Build_Profile;
   for Create_Missing_Dirs use "True";
   for Exec_Dir use "bin";
   for Main use ("rongen.adb");

   package Compiler is
      for Default_Switches ("Ada") use Rongen_Config.Ada_Compiler_Switches & ("-gnat2022");
   end Compiler;

   package Binder is
      for Switches ("Ada") use ("-Es"); --  Symbolic traceback
   end Binder;

   package Install is
      for Artifacts (".") use ("share");
   end Install;

end Rongen;
